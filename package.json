{
  "name": "Teayudo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd front && npm install && npm run build",
    "start": "cd back && npm install && npm start"
  },
  "engines": {
    "node": "20.3.1"
  }
}
